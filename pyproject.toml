[project]
name = "study-with-ai-2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "fun5307", email = "fun5307@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "pandas>=3.0.5",
    "playwright>=1.62.0",
    "python-dotenv>=1.2.3",
    "requests",
]

[project.scripts]
study-with-ai-2 = "study_with_ai_2:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
