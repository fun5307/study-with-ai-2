# Notion 학습 정리 가이드

이 문서는 사용자가 “오늘 배운 내용을 Notion의 Day N에 정리해 줘”라고 요청할 때 사용하는 전용 작업 기준이다.  
목표는 짧은 수업 요약이 아니라, 나중에 복습만으로도 코드의 흐름과 개념을 다시 이해할 수 있는 **상세 학습 노트**를 만드는 것이다.

## 1. 작업 시작 전 확인

1. Notion에서 대상 Day 페이지를 찾고, 반드시 현재 내용을 가져온다.
   - 기존 페이지가 비어 있으면 내용을 작성한다.
   - 이미 내용이 있으면 사용자가 요청한 범위만 갱신한다.
   - 제목의 Day 번호와 실제 수업 일자가 어긋날 수 있으므로, 제목만 보고 다른 페이지를 만들지 않는다.
2. 사용자가 참고하라고 한 이전 Day 페이지(기본: Day 14~16)를 읽는다.
3. 사용자가 지정한 노트북·소스 파일을 모두 읽고, 파일별 주제·코드 흐름·실습 결과·오류를 파악한다.
4. 사용자가 대화 중 특별히 강조한 오류, 수정 이유, 구현 판단은 Troubleshooting 또는 기억할 점에 반영한다.

## 2. 기본 문서 구조

아래 순서를 기본으로 한다. 수업 내용에 맞지 않는 항목만 생략한다.

````markdown
# Day N — 핵심 주제 2~4개

오늘은 ...을 배웠다.

```plain text
전체 처리 흐름
```

---

# 1. 파일명 — 주제 {toggle="true"}
    ## 이 파일에서 배운 것
    ## 왜 필요한가?
    ## 코드와 연결
    ### 1) 단계 이름
    ```python
    실제 핵심 코드
    ```
    - 코드의 입력, 출력, 핵심 변수 설명
    - 이 단계가 다음 단계와 연결되는 방식
    ### 2) 다음 단계
    ...

---

# 핵심 용어 한 번에 보기 {toggle="true"}

## 오늘의 자가 점검
````

- **노트북 또는 사용자가 지정한 소스 파일 하나당 최상위 토글 하나**를 만든다.
- 파일 순서는 사용자가 준 순서를 지킨다.
- 파일 하나의 내용이 길면, 그 파일 토글 안에서 `##`, `###`로 다시 나눈다.
- Notion Enhanced Markdown의 토글 문법은 반드시 `{toggle="true"}`를 사용한다.

## 3. 가장 중요한 원칙: 내용과 코드를 충분히 연결하기

Day 14~16 형식의 장점은 개념 설명만 하거나 코드만 붙여 넣지 않고, 둘을 바로 이어 준다는 점이다. 각 파일에서는 다음 세 요소를 모두 쓴다.

### A. 개념: 무엇이고 왜 필요한가?

- 새 용어는 처음 나올 때 쉬운 한 문장 정의를 쓴다.
- “무엇을 한다”에 더해 “왜 이 단계가 필요한가”를 설명한다.
- 이전 단계의 한계가 다음 단계를 왜 필요하게 했는지 연결한다.

예:

- 청킹: 긴 문서를 검색하기 적합한 작은 조각으로 나누는 작업이다. 한 문서에 여러 주제가 섞이거나 모델 입력 한도를 넘는 문제를 줄인다.
- Retriever: 질문을 받아 관련 `Document` 목록을 반환하는 검색기다. 벡터 DB 자체와 달리 RAG 체인에 바로 연결하기 좋은 인터페이스다.

### B. 코드: 실행 흐름이 보이는 핵심 코드

- 실제 노트북 코드에서 핵심 부분을 사용한다. 존재하지 않는 예제나 API를 만들어 넣지 않는다.
- import만 따로 길게 나열하지 말고, 그 import가 처음 필요한 코드와 가깝게 둔다.
- 한 코드 블록에는 하나의 목적만 담는다.
- 너무 긴 코드는 핵심 줄을 중심으로 잘라 제시하되, 흐름이 끊기면 안 된다.
- 코드 속 핵심 키·변수·메서드는 바로 아래에서 설명한다.

설명해야 할 항목 예시:

- 입력값과 반환값
- 중요한 변수의 역할
- 메서드 호출 순서
- 다음 단계가 이 결과를 어떻게 사용하는지
- 자주 헷갈리는 옵션의 차이

### C. 해석: 이 코드가 실제로 어떻게 동작하는가?

코드 블록 아래에 최소한 다음을 쓴다.

1. 코드가 받아들이는 입력
2. 코드가 만드는 중간 결과
3. 다음 단계로 넘기는 값
4. 이 방식의 이유 또는 주의점

나쁜 예:

```markdown
Retriever를 만들었다.
```

좋은 예:

```markdown
`as_retriever()`는 벡터 DB를 질문 문자열 → 관련 `Document` 목록 형태로 바꾼다.
`k`는 최종 문서 수이고, MMR을 쓸 때 `fetch_k`는 먼저 비교할 후보 수다.
검색 결과는 아직 문자열이므로, 다음 단계에서 `format_docs()`가 본문과 metadata를 합쳐 context로 바꾼다.
```

## 4. 파일별 상세화 기준

파일의 수만큼 토글을 만들었다고 끝이 아니다. 각 토글은 해당 파일을 다시 열지 않아도 복습할 수 있을 만큼 충분히 상세해야 한다.

### 문서 로드·데이터 전처리 파일

다음을 설명한다.

- 데이터 원본 형식(PDF, CSV 등)과 읽는 방법
- `Document(page_content, metadata)`에서 본문과 metadata의 역할
- 결측치·텍스트 길이·인덱스처럼 이후 오류가 될 수 있는 데이터 특성
- 청킹을 한다면 chunk size, overlap, 분할 방식의 의미
- 원본 문서와 청크 중 무엇을 저장했는지와 그 이유

### 벡터 DB·검색 파일

다음을 설명한다.

- 임베딩 모델과 벡터 DB의 역할 차이
- 저장 경로, 컬렉션 이름, 영속 저장의 의미
- 유사도 검색과 MMR 검색의 차이
- `k`, `fetch_k`, `lambda_mult` 같은 검색 옵션의 의미
- 검색 결과가 `Document` 목록이며, context 문자열로 바뀌어야 하는 이유

### LangChain·LCEL 파일

다음을 설명한다.

- Prompt, Model, Output Parser를 분리하는 이유
- `ChatPromptTemplate.from_template()`와 `from_messages()`의 사용 맥락
- system 메시지와 human 메시지의 역할
- `|`가 앞 단계의 출력을 다음 단계 입력으로 넘기는 방식
- `invoke()`에 전달해야 하는 입력 딕셔너리 구조
- `StrOutputParser`, 목록 parser, Pydantic parser, `with_structured_output()`의 차이

### RAG 통합 파일

반드시 질문의 두 갈래 흐름을 글이나 다이어그램으로 표시한다.

```plain text
질문
├─ retriever → format_docs → context
└─ RunnablePassthrough → question

{context, question} → prompt → model → parser
```

- `context`에는 검색된 근거가 들어간다.
- `question`에는 사용자의 원래 질문이 들어간다.
- `RunnablePassthrough()`가 왜 필요한지 설명한다.
- 프롬프트의 자리 표시자와 딕셔너리 키 이름이 일치해야 함을 설명한다.

## 5. Troubleshooting 작성 기준

Troubleshooting은 실제로 겪은 중요한 문제만 기록한다. 일반 개념 설명보다 길어지면 안 된다.

각 항목은 아래 네 줄 안팎을 기본으로 한다.

```markdown
### 오류 이름 또는 증상

- **증상**: 실제 오류 메시지 또는 관찰한 현상
- **원인**: 어느 값·파일·실행 순서가 왜 문제였는지
- **해결**: 다시 실행할 셀, 바꿔야 할 설계, 확인 방법
- **예방**: 다음에 같은 문제를 피할 규칙
```

특히 RAG 실습에서는 다음을 정확히 구분한다.

- Retriever 객체를 검색 결과 리스트로 덮어쓴 문제
- `metadata["page"]`와 `metadata["질문번호"]`처럼 데이터마다 키가 다른 문제
- 같은 Chroma 저장 경로·컬렉션에 반복 적재해 이전 문서가 누적되는 문제
- 실행 중인 커널이 DB 파일을 잡고 있어 Windows에서 폴더를 이동·삭제·이름 변경하지 못하는 문제
- 옵션 오타(`lambda_mult` 등)로 의도한 설정이 적용되지 않는 문제

## 6. 분량과 비중 규칙

- 핵심 개념·코드 설명(파일별 1~5 등)이 문서의 대부분을 차지해야 한다.
- 실습 Troubleshooting은 보통 전체의 15~25%를 넘기지 않는다.
- “너무 짧은 요약”을 피한다. 중요한 파일에는 코드 흐름을 2~4개 이상의 단계로 나눠 설명한다.
- 반대로 출력 결과 전체, 대량 데이터, 긴 오류 스택 트레이스는 복사하지 않는다.
- 같은 설명을 여러 토글에 반복하지 않는다. 앞 파일에서 설명한 개념은 이후 파일에서 그 파일의 새 활용 방식에 집중한다.

## 7. 한국어 작성 스타일

- 설명은 한국어로 쓴다.
- 학습자가 복습하는 문서이므로 단정하고 친절하게 쓴다.
- 어려운 용어는 첫 등장 때 영어 원문과 짧은 정의를 함께 쓴다.
- 코드 블록 아래 설명은 문장과 불릿을 섞어, 읽기 편하게 작성한다.
- 이모지는 페이지 아이콘 외에는 과도하게 사용하지 않는다.
- 사실과 코드를 구분한다. 실행하지 않은 결과나 검증하지 않은 수치를 적지 않는다.

## 8. 작성 후 검증 체크리스트

- [ ] 대상 Day 페이지를 직접 가져와 기존 형식과 위치를 확인했는가?
- [ ] 사용자가 지정한 모든 파일이 순서대로 토글 하나씩 있는가?
- [ ] 각 파일 토글에 개념, 핵심 코드, 코드 해석이 모두 있는가?
- [ ] 1~5 핵심 학습 내용이 Troubleshooting보다 충분히 상세한가?
- [ ] 코드의 변수명·경로·클래스·메서드가 실제 소스와 일치하는가?
- [ ] metadata 키와 출처 표기 규칙을 데이터에 맞게 썼는가?
- [ ] RAG 흐름에서 context와 원래 question의 경로를 설명했는가?
- [ ] Troubleshooting은 실제 문제만 간결하게 적었는가?
- [ ] Notion 업데이트 후 다시 가져와 제목·토글·주요 섹션이 정상 반영됐는지 확인했는가?
